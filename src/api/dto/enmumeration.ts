/**
 * 归属类型枚举
 */
export enum OwnerTypeEnum {
  PERSONAL = 'PERSONAL',
  ORGANIZATION = 'ORGANIZATION',
}

/**
 * 可见类型枚举
 */
export enum VisibleTypeEnum {
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE',
  OPGANIZATION_VISIBLE = 'POGANIZATION_VISIBLE',
}
